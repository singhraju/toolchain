apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: simple-task
spec:
  params:
    - name: MESSAGE
      type: string
      description: The message to echo
  steps:
    - name: echo-message
      image: ubuntu:22.04
      script: |
        #!/bin/bash
        echo "Message: $(params.MESSAGE)"
