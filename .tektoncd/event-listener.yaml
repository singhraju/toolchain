# event-listener.yaml for hello world
apiVersion: tekton.dev/v1beta1
kind: EventListener
metadata:
  name: event-listener
spec:
  triggers:
    - name: trigger
      bindings:
        - ref: trigger-binding
      template:
        ref: trigger-template
