# .tekton/pipelinerun.yaml
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: hello-world-run
spec:
  pipelineRef:
    name: lambda-deploy-pipeline   # match your pipeline name
  workspaces:
    - name: ws
      emptyDir: {}
